# Ralph Loop Progress Log

Each iteration appends one line. Format:

```
<iso-utc> task <id> <status> — <short note>
```

---

2026-04-21T04:14:00Z loop-bootstrap — Implementation plan, prompt, status, progress log created. Ready to launch.
2026-04-21T05:42:00Z task 1 completed — Next 15.5 + React 19.2 + TS 6 + Tailwind 4 + Vitest 4 installed; route groups scaffolded; design-language tokens encoded; smoke test (12 assertions) green; all qa gates pass.
2026-04-21T05:46:30Z task 2 completed — Drizzle + postgres-js + @neondatabase/serverless installed; schema registry + client + config + .env.example + drizzle/README; 5 new tests RED-then-GREEN; 17/17 pass; lint + typecheck clean. Real Neon connection is human-action.
2026-04-21T05:51:30Z task 3 completed — @aws-sdk/client-s3 + presigner installed; StorageAdapter contract with case-scoped key layout; in-memory adapter with version history; S3 adapter for R2/MinIO/S3; factory via STORAGE_DRIVER env; 17 new tests RED-then-GREEN; 34/34 pass; lint + typecheck clean. Real R2/MinIO is human-action.
2026-04-21T05:58:00Z task 4 completed — inngest@4.2 installed; typed event catalog via eventType+staticSchema; smoke workflow with handler exported separately for unit tests; Next /api/inngest route via inngest/next; npm run dev runs Next+Inngest dev in parallel via npm-run-all2; 4 new tests RED-then-GREEN; 38/38 pass; lint + typecheck clean. Live workflow firing is human-action (start dev server).
2026-04-21T06:01:45Z task 5 completed — @sentry/nextjs + @axiomhq/js installed; Logger + ErrorTracker interfaces; Axiom logger; Sentry tracker with severity-level mapping; no-op fallbacks for missing env; cached factory with reset for tests; 6 new tests RED-then-GREEN; 46/46 pass; lint + typecheck clean. Real Sentry/Axiom keys are human-action.
2026-04-21T06:06:30Z task 6 completed — @playwright/test installed; playwright.config.ts with anonymous+customer+ops-staff role-scoped projects; sample anonymous spec; per-worker DB schema isolation wired into vitest setupFiles; .github/workflows/ci.yml runs qa+e2e jobs; 6 new tests RED-then-GREEN; 51/51 pass; lint + typecheck clean. Foundation wave (1-6) complete.
2026-04-21T06:11:00Z task 7 completed — USER-TEST checkpoint #1. npm run build succeeds end-to-end (8 routes); npm run db:generate runs; 52/52 tests; lint + typecheck clean. Caught + fixed: 3 route groups colliding on /, typedRoutes warning, multi-lockfile warning, next-env.d.ts lint error. Human still owes live-env verification (dev server, real DB migration, real R2 upload, live Inngest fire, real Sentry/Axiom event, CI green on real PR).
2026-04-21T06:16:30Z task 8 completed — @clerk/nextjs + @clerk/testing installed; ClerkProvider in root layout; middleware gates /app /ops /api/cases /api/uploads with redirectToSignIn; pure route-gating predicates; themed sign-in/sign-up pages; Actor type surface; Playwright protected-redirect spec (skipped without Clerk env); 29 new tests RED-then-GREEN; 81/81 pass; build green with stub Clerk env; lint + typecheck clean. Real Clerk keys are human-action.
2026-04-21T06:21:00Z task 9 completed — STAFF_ROLES enum + can() permission matrix (single chokepoint for authorization); middleware extended to gate /ops/** via org_role claim with bounce to /app for non-staff; ops-redirect Playwright spec (skipped without env); 12 new tests RED-then-GREEN; 93/93 pass; build green; lint + typecheck clean. Real Clerk org config + MFA is human-action.
2026-04-21T06:26:30Z task 10 completed — Actor resolver (pure session→Actor + Next-runtime wrapper); requireActor/requireCan/requireStaff guards with AuthError abstract base + 401/403 mapping; integration test demonstrates guarded-route-handler pattern across anon/customer/analyst/validator/admin; 23 new tests RED-then-GREEN; 116/116 pass; lint + typecheck clean. Local DB row lookup ships in task #11.
2026-04-21T06:35:00Z task 11 completed — Drizzle identity schema (customers + staff_users with UNIQUE clerk_user_id); first migration drizzle/0000_identity.sql; IdentityRepo contract + in-memory + Drizzle implementations; handleClerkEvent dispatcher (idempotent, deactivate-not-delete on membership removal); /api/webhooks/clerk route with svix verification + observability hooks; 14 new tests including REPLAY assertions; 130/130 pass; lint + typecheck clean; build green. Wave 2 (auth + roles) complete.
2026-04-21T06:46:00Z task 12 completed — Loaded /taste-skill→minimalist-ui per surface map; 7 design-system primitives (Button solid+underline, Card, StatusBanner, Eyebrow, Hairline, Footnote+Content, KbdShortcut); /ui-kit page renders every primitive; Playwright baseline spec; @vitejs/plugin-react wired for tsx tests; 16 new component tests RED-then-GREEN; 146/146 pass; lint + typecheck clean; build green (/ui-kit static at 141B).
2026-04-21T06:50:00Z task 13 completed — USER-TEST checkpoint #2. Build green (10 routes), 146/146 pass, design-language banned-pattern grep clean (no Inter/Roboto/shadow-md|lg|xl/rounded-full in primitives). Human owes live Clerk + design-eyeball verification.
2026-04-21T06:58:00Z task 14 completed — Homepage built per PRD 05 hierarchy: HomeHero (editorial GT Sectra display + single accent CTA), HomeProof, HomeMovements (3 hairline-bordered cards with mono prices), HomeAntiPositioning (real-text non-goals), HomePullQuote (canonical trust posture); marketing layout adds SiteFooter (3 nav columns + 'Not legal advice' real-text disclosure); 11 new component tests + Playwright spec; 157/157 pass; lint + typecheck clean; build green (/ static-prerendered at 143B).
2026-04-21T07:04:00Z task 15 completed — Editorial /how-it-works long-read with 3 stage movements (mono price, what-we-do/what-you-do parallel columns, display-face artifact callout, labeled hairline section breaks); closing CTA to /screener; 4 component tests + 1 Playwright spec; 161/161 pass; lint + typecheck clean; build green (/how-it-works static at 146B). Note: task #16 (/pricing) blocked on #36 (Stripe); next iteration picks #17 (/trust).
2026-04-21T07:09:00Z task 17 completed — Two long-form editorial trust pages. /trust: canonical promise verbatim + 5 non-goals + collect/retention/rights/security/compliance + real-footnote disclosure on 15-min upload expiry. /trust/sub-processors: typeset table of 11 vendors with Phase-2 flagging. 9 new component tests + 1 Playwright spec; 170/170 pass; lint + typecheck clean; build green (both pages static at 151B).
2026-04-21T07:16:00Z task 18 completed — Disclosure module with frozen canonical constants (string-equality tests freeze the wording); TrustFootnote component (inline + asFooter modes); SiteFooter refactored to use it; new (app)/layout + (ops)/layout wrap their pages with the disclosure footer; integration test asserts disclosure on every marketing page; 7 new tests RED-then-GREEN; 180/180 pass; lint + typecheck clean. Note: task #19 (USER-TEST staging review) blocked because task #16 (/pricing) blocked on #36 (Stripe); loop skips ahead to #20 (Screener domain logic).
2026-04-21T07:24:00Z task 20 completed — Screener domain (5 modules: types, questions, branching, estimator, qualification + index). Versioned ESTIMATOR_VERSION=v1 + RESULT_VERSION=screener-v1+estimator-v1 for audit. 38 new tests covering every branch / band / confidence / qualification / recoveryPath / next-step. Coverage 96% lines / 95.9% branches / 100% functions on the screener context — exceeds ≥95% acceptance. 218/218 pass; lint + typecheck clean; build green.
2026-04-21T07:32:00Z task 21 completed — Screener UI: /screener (focused single-col flow with TrustFootnote, no SiteFooter); QuestionPrompt (display H1 + mono Q-of-10, no progress bar); AnswerInput (per-kind: yes/no, yes/no/unknown, country, choice-lists, multi-category checkboxes, email_capture with validation); ScreenerFlow orchestrator (sessionStorage persistence + back-button); inline result card; 18 new tests + 1 Playwright spec; 238/238 pass; lint + typecheck clean; build green (/screener 4.79kB / 107kB first-load — first interactive client route). jest-dom matchers wired via new setupFile.
2026-04-21T07:34:00Z task 22 completed — Admin closure. estimator.ts shipped inside task #20's bundle; tests cover all bands + confidence tiers. No new code needed. 238/238 still passing. Next: task #23 (magic-link) is task-blocked on #27 (Resend); loop pivots to #27.
2026-04-21T07:42:00Z task 27 completed — resend + @react-email/components + render installed. EmailTransport contract; Resend transport (idempotency + error normalization); console-transport dev/test fallback; cached factory enforces EMAIL_FROM presence. renderEmail() returns HTML + plaintext. EmailLayout wraps every template with the canonical NOT_LEGAL_ADVICE_DISCLOSURE verbatim from the Disclosure module (single source). ScreenerResultsEmail first template ready for #23 + #28. 8 new tests RED-then-GREEN; 246/246 pass; lint + typecheck clean; build green.
2026-04-21T07:53:00Z task 25 completed — screener_sessions + leads Drizzle schema (UNIQUE(email, session) backs idempotency); drizzle/0001_screener.sql generated; ScreenerRepo contract + in-memory + Drizzle impls; PUBLIC SURFACE SPLIT — index.ts UI-safe, server.ts with `import 'server-only'` exposes the repo factory; 12 new tests RED-then-GREEN; 254/254 pass; lint + typecheck clean; build green (/screener still 4.79kB — no server code in client bundle). Backlog correction: task #23 deps updated to include #25.
2026-04-21T08:05:00Z task 23 completed — magic-link tokens (HMAC-SHA256 + 7d TTL + constant-time compare); finalize.ts pure server function (idempotent lead, replay-safe email via idempotencyKey, DQ paths skip lead+email); POST /api/screener/complete (Zod-validated, optional Turnstile, observability hooks); /screener/results?token=… server-rendered with friendly expired/tampered fallback; ScreenerFlow callback now (result, answers); ESLint allows @contexts/<name>/server (ADR 001 amendment); 9 new tests RED-then-GREEN; 263/263 pass; lint + typecheck clean; build green (16 routes, /screener bundle stays at 4.94kB).
2026-04-21T08:11:00Z task 24 completed — ResultsDossier single canonical photographable surface (Berkeley Mono hero metric with thousands separators, customs-orange uppercase confidence, prerequisites checklist Met/Missing, recommended next step + magazine-underline CTA, disqualified variant with opt-in language). /screener + /screener/results both refactored to delegate to it. 8 new tests RED-then-GREEN; 271/271 pass; lint + typecheck clean; build green (/screener bundle 5.67kB).
2026-04-21T08:15:00Z task 26 completed — USER-TEST checkpoint #2 (real screener walkthrough). Implementation-level: 16 routes, 85 screener-related tests pass, full magic-link flow works. Wave 4 (Eligibility screener) complete. Human owes live walkthrough with real importers + a11y audit + photographable confirmation + live Resend delivery.
2026-04-21T08:27:00Z task 28 completed — Lifecycle workflow #1: screener-completed. Email send moved from inline (in finalize.ts) to durable Inngest workflow triggered by platform/screener.completed event. Two-step handler (render-email + send-email) with idempotencyKey for retry safety. finalize.ts publishes via callback; route handler wires inngest.send. Workflows registry updated. server-only aliased to a no-op in vitest config. 2 new tests + finalize tests refactored; 273/273 pass; lint + typecheck clean; build green.
2026-04-21T08:35:00Z task 29 completed — Lifecycle nudge cadence (24h + 72h). nudgeCadenceWorkflow with two step.waitForEvent windows listening for platform/payment.completed (filtered to sessionId) — cancels on purchase, fires per-cadence nudge with sessionId-scoped idempotencyKey otherwise. Two new templates (ScreenerNudge24hEmail soft-recovery + ScreenerNudge72hEmail founder-style records framing). platform/payment.completed event added (Stripe webhook in task #33 publishes). 4 new tests; 277/277 pass; lint + typecheck clean; build green.
2026-04-21T08:42:00Z task 31 completed — 6 new lifecycle templates (#4-#9): RecoveryPurchased, RecoveryMissingDocs (15-min call offer), EntryListReady (entry count + Prep next), PrepReady (Readiness Report + Concierge upsell), ConciergeUpsell (success-fee mechanic), Reengagement (day-14 soft re-entry). Shared editorial primitives (H1, P, PrimaryCta, SecondaryLink, greetingFor) extracted to _components.tsx. All wrap EmailLayout for canonical disclosure. 6 new render tests; 283/283 pass; lint + typecheck clean; build green. Templates wait for their publishing workflows in the Phase-1 ops-scaling tasks.
2026-04-21T08:50:00Z task 33 completed — Stripe SDK + webhook handler with idempotent dedupe. processed_stripe_events table (event_id PK, ON CONFLICT DO NOTHING); BillingRepo contract + in-memory + Drizzle; handleStripeEvent dispatcher (mark-then-dispatch, REPLAY-safe); checkout.session.completed publishes platform/payment.completed; POST /api/webhooks/stripe with signature verification + observability hooks; public-surface split (index UI-safe + server with Stripe client). drizzle/0002_billing.sql generated. 9 new tests including explicit REPLAY assertion; 292/292 pass; lint + typecheck clean; build green (17 routes).
2026-04-21T08:56:00Z task 34 completed — Pricing ladder in code per PRD 06. PRICE_LADDER (6 SKUs × 2 tiers, USD cents); SUCCESS_FEE_RATES (SMB 10–12%, mid 8–10%); SUCCESS_FEE_HARD_CAP $50k (blanket dollar cap — simplest defensible policy); determineTier (strict > thresholds, boundary lands SMB); priceFor + computeSuccessFeeCents (rate clamped into band, rounded to whole cents, zero on non-positive refund). Re-exported from src/contexts/billing/index.ts (UI-safe). 27 new tests including ladder-table coverage, tier-boundary, rate-clamp (low/high/default), $50k cap on $1M refund, fractional-cent rounding, zero/negative refund. 319/319 pass; lint + typecheck clean; build green (17 routes).
2026-04-21T09:10:00Z task 35 completed — Stripe catalog sync script (npm run stripe:sync). planCatalogSync(snapshot)→Plan is pure: diffs PRICE_LADDER against a Stripe snapshot, tags products with metadata.app=tariff-refund-request, uses lookup_key=trr__<sku>__<tier> for stable lookup, archives stale prices on amount drift, archives obsolete products when SKUs leave the ladder, ignores products owned by other apps. SKU_RECURRENCE: monitoring=monthly, others=one_time. executeCatalogPlan(plan, client) applies via narrow StripeCatalogClient surface (real impl in stripe-catalog-client.ts). Re-running on the resulting snapshot returns isNoOp=true — idempotency property frozen by tests. CLI handles missing STRIPE_SECRET_KEY gracefully. tsconfig.scripts.json stubs server-only via the vitest noop so tsx can import @contexts/billing/server. 17 new tests; 336/336 pass; lint + typecheck clean; build green (17 routes, /screener still 5.67kB — no Stripe SDK in client bundle).
2026-04-21T09:25:00Z task 36 completed — POST /api/checkout opens Stripe Checkout per (sku, tier). Pure buildCheckoutSessionParams (mode=payment for one-time, mode=subscription for monitoring; metadata.{screenerSessionId,sku,tier,app} for webhook routing; subscription_data.metadata mirror; success/cancel URLs from request Origin; automatic_tax enabled per PRD 06; allow_promotion_codes for the discount code edge). buildIdempotencyKey scoped to (sku, tier, screenerSessionId) — client retries safe. createCheckoutForSku resolves price by lookup_key (trr__<sku>__<tier>); never hardcodes Stripe price IDs; throws a clear error pointing at npm run stripe:sync if catalog is stale. Stripe-backed adapter (createStripeCheckoutClient) in checkout-client.ts (server-only). Route validates via Zod; 400/502/500 mapped. Stripe.Checkout.SessionCreateParams namespace doesn't resolve through TS 6 — switched to Parameters<Stripe['checkout']['sessions']['create']>[0] inference (version-stable). 24 new tests (13 builder + 4 adapter + 7 integration); 360/360 pass; lint + typecheck clean; build green (18 routes; /screener still 5.67kB).
2026-04-21T09:35:00Z task 39 completed — Drizzle schema for cases + audit_log per PRD 04. cases columns: id (cas_*), customerId (FK set null), screenerSessionId (FK set null), state (enum of all 18 PRD-04 states, default new_lead), tier, ownerStaffId (FK set null), createdAt/updatedAt + indexes on state/owner/customer. audit_log columns: id (aud_*), caseId (FK→cases RESTRICT NOT NULL), actorId, kind, fromState/toState (nullable enum), payload jsonb default empty object, occurredAt + indexes on (case,time)+kind. drizzle/0003_cases_audit_log.sql generated then hand-augmented with audit-log RLS DDL: ENABLE + FORCE row level security; SELECT/INSERT open; UPDATE+DELETE pinned to USING (false) so the database itself rejects modification. Migration name normalized in _journal.json. CASE_STATES + Row types re-exported from schema barrel for downstream contexts. 8 new tests including SQL-grep that asserts FOR DELETE USING (false) policy is present. 368/368 pass; lint + typecheck clean; build green (17 routes — schema is server-only).
2026-04-21T09:43:00Z task 16 completed — /pricing page per PRD 05 + PRD 06. Stage-by-stage editorial ladder rendered straight from PRICE_LADDER + SUCCESS_FEE_RATES + SUCCESS_FEE_HARD_CAP — no hand-typed numbers; an it.each table-test against priceFor(sku, tier) keeps the property honest. Three stages (00 free screener, 01 recovery, 02 prep) + Concierge as a separate region with full success-fee disclosure (10–12% SMB, 8–10% mid-market, $50k cap, against the realized refund) + Monitoring section pinned at the bottom with /mo suffix. data-sku per row + data-price-mono on figure cells for deterministic test resolution. Hairline rules, mono numerics in the Berkeley Mono fallback chain, no popular badges, no contact-sales CTAs, free tier rendered with same weight as paid. Added to footer-presence integration test (canonical "Not legal advice" disclosure asserted via the layout) + Playwright spec. 25 new tests; 393/393 pass; lint + typecheck clean; build green (19 routes — /pricing static at 166B + 102kB first-load).
2026-04-21T09:46:00Z task 19 completed — USER-TEST checkpoint #3 (marketing site live at staging). Implementation-side scaffolding done: all 6 marketing surfaces live (/, /how-it-works, /pricing, /trust, /trust/sub-processors, /ui-kit), all static at 166B + 102kB first-load. footer-presence integration test asserts canonical "Not legal advice" disclosure on every marketing page via the layout wrap. Playwright specs cover home/how-it-works/pricing/trust. Design-language banned-pattern grep clean. 393/393 tests pass; lint + typecheck clean; build green. Human owes: staging deploy on licensed-fonts build; founder + outside reviewer walkthrough; Lighthouse a11y/SEO/perf ≥95.
2026-04-21T09:54:00Z task 40 completed — XState v5 case machine in src/contexts/ops/case-machine.ts encoding all 18 PRD-04 states + every guarded transition. Pure (no I/O); runs via createActor() inside nextState(current, event). XState symbols private to the file (per ADR 008 warning); public surface via @contexts/ops: CaseState, CaseEvent, ActorRef, StaffRole, CASE_INITIAL_STATE, nextState, isValidTransition. Hard rules at the guard level: VALIDATOR_SIGNED_OFF→submission_ready requires actor.role===validator (admin/coordinator/analyst denied — no Readiness Report ever leaves QA without a real validator); CUSTOMER_FILED never auto-fires. Stall handling: STALL_DETECTED valid from in-progress states; STALL_RESUMED carries resumeTo pinned by RESUMABLE_STATES. disqualified is terminal-with-opt-in via REENGAGEMENT_OPT_IN→new_lead. closed fully terminal. Invalid transitions are no-ops. 30 new tests including full transition table via it.each + validator-role guard for every other staff role + stall/resume guards + closed-is-terminal sweep. xstate@^5 added as a dependency. 423/423 pass; lint + typecheck clean; build green (19 routes — machine is server-only).
2026-04-21T10:01:00Z task 41 completed — Case service transition() with audit logging. CaseRepo contract + in-memory + Drizzle implementations. recordTransition is the single transactional write per PRD 04 (UPDATE cases.state + INSERT audit_log in db.transaction; UPDATE includes WHERE state=$from for optimistic concurrency, stale callers fail row-count check + roll back with "drifted" error). transition({caseId, event, actor}, deps) loads the case, runs the machine, distinguishes invalid_transition from guard_rejected via synthetic-validator probe on VALIDATOR_SIGNED_OFF, persists, then publishes platform/case.state.transitioned. Publish failure does NOT roll back the DB write — the transition has happened; Inngest retries are observable (tested explicitly). Actor enrichment: callers pass actor on the input; transition() lifts it onto VALIDATOR_SIGNED_OFF so the guard sees it. payload column strips actor (PII on actorId only). Public surface: transition + types via @contexts/ops; getCaseRepo + drizzle factory via @contexts/ops/server. 10 new tests; 433/433 pass; lint + typecheck clean; build green (19 routes).
2026-04-21T10:08:00Z task 42 completed — Audit log mirror to Axiom per ADR 013. auditLogMirrorWorkflow listens on platform/case.state.transitioned and emits a structured info-level log "audit_log.mirror" with caseId/auditId/kind/from/to/actorId/occurredAt — Axiom logger ingests these into the observability dataset for compliance + analytics. Best-effort: no AXIOM_TOKEN → no-op logger short-circuits to mirrored=false (workflow succeeds, no retry); live Axiom throws → rethrow so Inngest retries with backoff. NEVER blocks the transactional case-state write — #41 commits before publishing the event. caseStateTransitioned event schema extended with auditId, kind, occurredAt; transition() threads them into the publish payload. Workflow registered in shared/infra/inngest/workflows + re-exported from @contexts/ops/server. 3 new audit-mirror tests + 1 transition test updated. 436/436 pass; lint + typecheck clean; build green (19 routes).
2026-04-21T10:13:00Z task 43 completed — USER-TEST checkpoint #4 (state machine governance). Static-analysis sweep completed + codified as a permanent test (tests/integration/governance/case-state-writes.test.ts). Two assertions enforced on every CI run: (1) no source file outside @contexts/ops/ mutates cases.state via INSERT/UPDATE/.set({state:...})/property assignment; (2) no source file outside @contexts/ops/ value-imports the cases table (type-only imports for CaseRow allowed). Walk allowlist covers the schema definition + barrel + schema-level smoke tests; everything else is policed. Current tree passes both — only @contexts/ops/drizzle-repo.ts writes cases.state via recordTransition inside db.transaction(). Audit log captures every transition (#41 happy-path + actor enrichment + audit-mirror tests; payload strips actor — PII on actorId only). 438/438 pass. Human owes: ops staff audit-log timeline UX review (blocked on task #82 — admin console build); Axiom mirror compliance-field check on a real dataset; sign-off on the governance test as the durable check.
2026-04-21T10:18:00Z task 44 completed — Drizzle schema for documents + recovery_sources per PRD 02 + PRD 07. documents columns: id (doc_*), caseId (FK→cases RESTRICT NOT NULL), storageKey (UNIQUE — one row per object in the bucket per ADR 006), filename, contentType, byteSize (bigint), sha256, uploadedBy (customer|staff|system), uploadedByActorId, version (default 1), supersedesId (lineage to prior version), createdAt. Documents are immutable; UNIQUE (case_id, sha256) for content-hash dedupe inside a case. recovery_sources columns: id (rsrc_*), caseId (FK→cases RESTRICT NOT NULL), path (broker|carrier|ace_self_export|mixed), kind (ace_export|broker_7501|broker_spreadsheet|carrier_invoice|paper_scan|other), documentId (FK→documents RESTRICT NOT NULL — provenance never optional per .ralph/PROMPT.md hard rule), uploadedBy + uploadedByActorId, confidence (pending|verified|rejected, default pending), notes, createdAt + indexes on (case,time)+document+confidence. drizzle/0004_documents_recovery_sources.sql generated; migration name normalized in _journal.json. Schema barrel re-exports row types + every enum constant for downstream contexts. 12 new tests covering registry membership, all columns, both enum sets, and migration SQL-grep checks (UNIQUE on storage_key + UNIQUE on (case_id, sha256)). 450/450 pass; lint + typecheck clean; build green (19 routes — schema is server-only).
2026-04-21T10:25:00Z task 45 completed — Pre-signed upload endpoint + recovery context. Two-step flow per PRD 02 (no orphan rows on mid-flight failures): POST /api/uploads validates (case-id shape, filename safety, content-type allowlist [PDF/XLSX/XLS/CSV/EML — no images per PRD 07], 50MB cap), generates documentId, returns the canonical case-scoped storage key + a 15-min pre-signed PUT URL — no DB write. Then POST /api/uploads/complete verifies object landed via headObject(), inserts the document row using bucket-reported size (not client). UNIQUE (case_id, sha256) dedup surfaces as outcome=duplicate_sha256 with the existing document — never a hard error. Recovery context split: validators + service via @contexts/recovery; storage + Drizzle repo via @contexts/recovery/server. Drizzle repo is race-aware (catch unique-violation, re-look-up). MAX_UPLOAD_URL_EXPIRY_SECONDS now re-exported from shared/infra/storage barrel. Status codes: 400/413/409. /api/uploads in PROTECTED_PREFIXES (middleware-gated). 34 new tests (16 validator + 10 service + 8 integration); 484/484 pass; lint + typecheck clean; build green (21 routes).
2026-04-21T10:32:00Z task 46 completed — Reusable UploadZone client component (src/app/_components/upload/UploadZone.tsx) per PRD 02 + design language. Drag-drop OR browse; multi-file; per-file preview row with filename + size in mono + status pill (Queued/Uploading/Uploaded/Duplicate/Failed); accent-colored slim 1px progress bar — no progress confetti, no big colored badges. Pre-validates content type + size BEFORE any network call (uses isAcceptedContentType + MAX_UPLOAD_BYTES from @contexts/recovery) so user sees rejection immediately. Retry button on failure. duplicate_sha256 → Duplicate status (not Uploaded). uploadFile() orchestrator in upload-client.ts is dep-injected (fetchFn + hashFile + putToBucket); defaults: crypto.subtle.digest("SHA-256") + native fetch PUT. Component takes Partial<UploadDeps> via `client` prop for test wiring/production swap. 15 new tests (6 client + 9 component): pre-validation paths skip network; single + multi-file happy paths; drag-drop event; failure-then-retry; duplicate_sha256. Component not yet wired into a page — recovery workspace lands in #51. 499/499 pass; lint + typecheck clean; build green (21 routes).
2026-04-21T10:38:00Z task 47 completed — DocumentViewer client component for the ops side-by-side workflow per PRD 04 (src/app/_components/document-viewer/DocumentViewer.tsx). Page navigation (prev/next buttons + ArrowLeft/ArrowRight when focused, disabled at boundaries), zoom (in/out clamped to MIN 0.5 / MAX 4.0 in 0.25 steps with disabled state at limits), live page + zoom indicators with aria-live=polite. PDF rendering decoupled via PdfLoader interface so jsdom tests stub the pdfjs runtime entirely. Production loader (createPdfjsLoader in pdf-loader.ts) lazy-imports pdfjs-dist + sets GlobalWorkerOptions.workerSrc=/pdf-worker.mjs. TODO(human-action) marker added: copy node_modules/pdfjs-dist/build/pdf.worker.min.mjs into public/pdf-worker.mjs at deploy time. Loading state ("Loading document…"), failure state (role=alert with error). pdfjs-dist@^4.10.38 added as dep. Highlight + copy-to-form interaction deferred to #51+. 8 new tests covering loading/render/error states, page nav, zoom + min/max clamp, aria-live indicators. 507/507 pass; lint + typecheck clean; build green (21 routes — component is in a route group).
2026-04-21T10:41:00Z task 48 completed — USER-TEST checkpoint #5 (upload + viewer flow). Implementation-side scaffolding: POST /api/uploads + POST /api/uploads/complete (#45) with 8 integration tests, UploadZone (#46) with 6 client + 9 component tests covering pre-validation/multi-file/drag-drop/retry/duplicate_sha256, DocumentViewer (#47) with 8 tests covering load/render/error states + page nav + zoom + aria-live. Provenance captured at schema level: documents.case_id NOT NULL, documents.uploaded_by + uploaded_by_actor_id recorded on every insert; recovery_sources.documentId NOT NULL (per the .ralph/PROMPT.md hard rule). UNIQUE (case_id, sha256) prevents accidental duplicates → outcome=duplicate_sha256. 507/507 pass. Human owes: copy pdfjs-dist worker into public/, provision R2 + CORS, upload real broker/ACE documents end-to-end, confirm provenance via audit log + recovery_sources rows once #51 (recovery workspace) and #82 (ops console) wire the components into pages.
2026-04-21T10:48:00Z task 49 completed — Recovery routing module per ADR 015 + PRD 02. src/contexts/recovery/routing.ts exposes determineRecoveryPath(answers) → RecoveryPath | null (pure: ClearancePath q4 → RecoveryPath; null on disqualification q1=no or q3=no) and recoveryPlanFor(path) → RecoveryPlan {outreachTemplate (subject + body with {{placeholders}}), acceptedDocs (DocumentKind[]), opsQueue (recovery-broker|recovery-carrier|recovery-ace), sla (firstTouchHours + completionHours), prerequisiteChecks ({id, label, required})}. Four plans (broker, carrier, ace-self-export, mixed), all frozen via toMatchSnapshot so any change is visible in PR review. UI + ops console MUST read from the plan, never branch on path per ADR 015 "no-recovery-path-conditionals" (custom lint enforcement deferred to #51+). qualification.ts refactored: inline mapRecoveryPath removed, computeResult now calls determineRecoveryPath from @contexts/recovery via the UI-safe surface (no ADR-001 violation). SCHEMA_TO_RECOVERY_PATH bridges the snake_case schema enum to ADR-015 kebab-case. 26 new tests (table-driven path + per-path queue/docs/SLA/template + snapshot freeze + non-empty invariant); existing 16 qualification tests still green. 533/533 pass; lint + typecheck clean; build green (21 routes).
2026-04-21T10:55:00Z task 50 completed — Outreach kit templates promoted to versioned per-path files (src/contexts/recovery/templates/{broker,carrier,ace,mixed}.ts + types.ts + index.ts). OUTREACH_TEMPLATE_VERSION="v1". renderOutreachKit(path, tokens) substitutes every {{placeholder}} via pure regex pass; throws on missing required placeholder so customer never sees {{brokerName}} literals embedded. routing.ts now consumes canonical templates via planTemplate(path) helper — single source of truth for outreach copy. Cross-template invariants enforced: every body {{placeholder}} declared in placeholders[]; every declared placeholder used at least once. Caught one orphan: mixed template body now references the date range so windowStart/windowEnd are not declared-but-unused. 4 frozen snapshots + 25 new tests; routing snapshot for mixed updated to reflect deliberate copy change. 558/558 pass; lint + typecheck clean; build green (21 routes).
2026-04-21T11:03:00Z task 37 completed — Payment aggregate + success-fee invoicing per PRD 06. Schema: payments table (id pay_*, caseId FK→cases RESTRICT NOT NULL, kind charge|refund|credit|success_fee_invoice, stripeEventId UNIQUE for idempotency [nullable for our own invoices], stripeChargeId/stripeInvoiceId, sku, amountUsdCents signed bigint [positive for charges/invoices, negative for refunds], currency, status, metadata jsonb, occurredAt + createdAt + indexes on (case,time)+kind). drizzle/0005_payments_aggregate.sql generated + name normalized. computeSuccessFeeInvoiceCents applies 3 clamps: (1) $50k hard cap, (2) subtract alreadyInvoicedSuccessFee so retried paid events are idempotent, (3) remaining refund headroom after Stripe refunds to customer. Never negative. generateSuccessFeeInvoice composes history-lookup + computation + insert + publish; returns outcome=created or skipped (no_remaining_fee). recordPayment idempotent on stripeEventId (race-aware Drizzle repo). In-memory + Drizzle repos. Public surface via @contexts/billing; server factories via /server. 18 new tests covering full clamp matrix + idempotency + happy path + zero-fee skip. 576/576 pass; lint + typecheck clean; build green (21 routes — schema is server-only).
2026-04-21T11:06:00Z task 38 completed — USER-TEST checkpoint #6 (end-to-end purchase in test mode). Implementation-side building blocks: stripe catalog sync CLI (#35), POST /api/checkout with 24 tests (#36), POST /api/webhooks/stripe with REPLAY-safe dedupe + Inngest platform/payment.completed publish (#33), Payment aggregate with 18 tests for idempotent recordPayment + 3-clamp success-fee invoicing (#37). GAP surfaced for the human walk: the webhook publishes platform/payment.completed and dedupes via processed_stripe_events but does NOT yet write a Payment row — webhook only has metadata.screenerSessionId (checkout fires before a case exists). Wiring recordPayment requires either (a) pre-checkout case creation with metadata.caseId, or (b) downstream Inngest workflow that creates the case on platform/payment.completed and writes the Payment row there. Plan: (b) lands with #52 (recovery workspace). 576/576 pass. Human owes: stripe:sync against test mode, browser checkout flow, Stripe CLI replay (verify second replay is no-op), Inngest dev UI confirmation that platform/payment.completed fires exactly once. Payment row + case state transition verifiable after #52.
2026-04-21T11:17:00Z task 51 completed — Customer recovery workspace UI at /app/case/[id]/recovery per PRD 02. Server component resolves recovery path via getCaseRepo().findCase → getScreenerRepo().findSessionById → determineRecoveryPath → recoveryPlanFor + renderOutreachKit. Three panes: Left RecoveryStatusPanel (case id, status banner with path label + SLA, document checklist per plan.acceptedDocs, prerequisite checks required vs optional, uploaded list); Center OutreachKitPanel (rendered subject + body verbatim, CopyButton client subcomponent writes "Subject: X\\n\\n<body>" to navigator.clipboard with "Copied" feedback, attachments-needed list, template version footer); Right UploadPanel wraps existing UploadZone with case-id binding. Zero UI conditionals on path per ADR 015 — all reads via the plan. 404 paths via notFound(): case doesn't exist; case has no recovery path (no screener session OR disqualified). not-found.tsx editorial fallback. 16 new tests (6 status-panel + 5 outreach-panel incl. clipboard write + 5 integration-page via stubbed repos). 592/592 pass; lint + typecheck clean; build green (22 routes — /app/case/[id]/recovery at 2.97kB + 114kB first-load, dynamic).
2026-04-21T11:24:00Z task 30 completed — Stalled-case cadence Inngest workflow per PRD 04. STALLED_CADENCE_STAGES=[stalled-48h (48h), stalled-96h (48h), stalled-day-7 (72h)] — additive timeouts hitting cumulative 48h/96h/168h milestones. stalledCadenceWorkflow triggers on platform/case.state.transitioned; handler short-circuits when event.data.to !== "stalled" (unrelated transitions burn no cadence work). Per stage: step.waitForEvent listens for same event filtered by `async.data.caseId == "<id>" && async.data.from == "stalled"`; on timeout, step.run fires cadence by appending stalled_cadence:<stage> audit row (fromState/toState null, kind discriminator). Resumption in any window stops the cadence with stage-specific cancelledBy. CaseRepo extended with appendAuditEntry — both in-memory and Drizzle (insert without state UPDATE). Workflow registered in shared/infra/inngest/workflows + re-exported from @contexts/ops/server. 7 new tests covering happy path (3 fires + 3 audit rows), per-window cancellation, audit shape, caseId-scoped filter expression. 599/599 pass; lint + typecheck clean; build green (22 routes — workflow is server-only).
2026-04-21T11:34:00Z task 32 completed — USER-TEST checkpoint #7 (lifecycle emails reviewed). Implementation-side governance baseline codified as a permanent test (tests/integration/governance/lifecycle-templates.test.tsx) — 7 assertions enforced on every CI run: (1) every v1 lifecycle template file exists; (2) every template imports EmailLayout (canonical "Not legal advice" disclosure wraps every send); (3) no AI copywriting clichés (Elevate, Seamless, Unleash, Next-Gen, Game-changer, Delve) or ad-style imperatives (Click here, Limited time, Act now); (4) rendered HTML carries the disclosure for every template; (5) no image-only bodies — real-text content >200 chars per template; (6) plain-text rendering non-empty; (7) plain-text also carries the disclosure (so non-HTML clients still see it). Also fixed a flaky DocumentViewer zoom test (waited for renderCalls.length>0 in addition to the page indicator). 606/606 pass. Human owes: founder + ops top-to-bottom read of all 9 templates; capture rewrites as new tasks. Governance test catches structural drift; human catches editorial drift.
2026-04-21T11:43:00Z task 52 completed — Ops case workspace at /ops/case/[id] per PRD 04. Three-pane: Left CaseHeaderPanel (case id, state pill with data-state attr, tier, recovery path, owner, queue, SLA hours, action panel with Claim + Mark stalled stubs); Center ExtractionFormPanel (entry-extraction form: entry number / entry date / IOR / duty / HTS codes; local-state save with onSave seam — persistence lands with #55 entries schema); Right DocumentViewerPanel (selectable doc list + existing DocumentViewer for focused PDF; j/k keyboard nav between docs with boundary clamps; ↑↓ inside viewer pages within doc). Server component resolves recovery path via getCaseRepo → findSessionById → determineRecoveryPath; 404 paths via notFound() for missing case or no resolvable path. Industrial-brutalist Swiss Industrial Print mode per PRD 04 — mono dominance, dense tabular dl, hairline borders, zero border-radius, uppercase tracking; no banned theatrics (no scanlines/halftone/hazard red). 18 new tests (5 header + 5 extraction + 5 viewer + 3 integration via stubbed repos). 624/624 pass; lint + typecheck clean; build green (23 routes — /ops/case/[id] at 3.08kB + 105kB first-load, dynamic).
2026-04-21T11:55:00Z tasks 53 + 55 completed (paired) — Manual entry-extraction form + provenance write per PRD 02 + PRD 07. Schema (drizzle/0006_entries_provenance.sql, satisfies #55): entries (id ent_*, caseId FK→cases RESTRICT NOT NULL, entryNumber, entryDate, IOR, dutyAmountUsdCents bigint, htsCodes text[], phaseFlag, validatedAt/validatedBy, createdAt/updatedAt + UNIQUE(case_id, entry_number) for canonical dedupe + index on (case,time)). entry_source_records (id esrc_*, entryId FK→entries RESTRICT NOT NULL, recoverySourceId FK→recovery_sources RESTRICT NOT NULL — provenance never null per .ralph/PROMPT.md hard rule, rawData jsonb, confidence enum default pending, extractedAt/extractedBy + indexes). EntriesRepo (in-memory + Drizzle) with saveExtractedEntry transactional contract — entry + source insert in one db.transaction. Service saveExtractedEntry composes entry write + audit_log row (kind=entry.extracted on first save; kind=entry.source_attached on second-source path). POST /api/cases/[id]/entries route: Zod-validated body, 400 on bad body, 404 on missing case, 200 with outcome+entry+sourceRecord+auditId. ExtractionFormPanel wired to POST when no onSave seam — requires recoverySourceId prop, shows error state if missing. 12 new tests (6 service + 6 integration route); existing form tests adapted to async save. 637/637 pass; lint + typecheck clean; build green (24 routes — added /api/cases/[id]/entries).
2026-04-21T12:00:00Z task 54 completed — USER-TEST checkpoint #8 (full recovery workflow). Implementation-side scaffolding for the four legs is complete: (1) Stripe Checkout (#36) — POST /api/checkout + REPLAY-safe webhook publishing platform/payment.completed; (2) Customer recovery workspace (#51) — /app/case/[id]/recovery 3-pane; (3) Ops workspace + extraction (#52, #53) — /ops/case/[id] 3-pane + POST /api/cases/[id]/entries with idempotent (case_id, entry_number) save; (4) Lifecycle email cadences (#28-#31) — workflows registered. GAPS for human walk (carry from #38): webhook does not yet write a Payment row OR auto-create a Case from platform/payment.completed; ExtractionFormPanel.recoverySourceId is currently a static prop. The orchestration workflow that closes both lands in v1-launch wave (#80+). 637/637 pass. Human owes: provision Stripe test/R2/Inngest/Postgres; manually create case+recovery_source; walk customer→upload→staff→extract; verify audit trail.
2026-04-21T12:05:00Z task 56 completed — Entry-number canonicalization function per PRD 07. canonicalizeEntryNumber(raw) accepts dashed/undashed forms, leading/trailing whitespace, en/em dashes (Excel/Word paste), spaces between segments, mixed case; returns canonical FFF-SSSSSSS-C uppercase or typed rejection {empty | length | filer_code_invalid | sequence_not_digits | check_digit_not_digit}. CANONICAL_ENTRY_NUMBER_RE freezes canonical shape; formatCanonicalEntryNumber throws loudly on non-canonical input. Check-digit math deferred to Phase 2 (v1 shape-only validation). When caller supplied dashes (3 raw segments), malformed filer surfaces as filer_code_invalid rather than generic length — more useful for analyst review queue. Entries context public surface at src/contexts/entries/index.ts. 24 new tests covering 7 happy formats + 11 rejection paths + raw preservation + canonical regex + display-form throw. 661/661 pass; lint + typecheck clean; build green (24 routes — pure module, no client-bundle impact).
2026-04-21T12:13:00Z task 57 completed — Dedupe + fuzzy match per PRD 07. classifyEntries(incoming, existing) returns one ClassifiedEntry per incoming row with one of five outcomes: exact_duplicate (canonical match against existing — caller attaches a second source), duplicate_in_batch (same canonical number repeats inside incoming list after first occurrence), fuzzy_review_pair (matches existing on date+IOR but DIFFERENT canonical number — both records kept; analyst review queue surfaces the pair per PRD 07 acceptance), new (no match), invalid (canonicalization failed — canonicalResult attached for the manual-correction queue). Pure function — composes with entries repo in #58+. Fuzzy key is (date + IOR-lowercase-trimmed); requires both halves; v1 uses exact-date match (date-window tolerance deferred to Phase 2). Exact match always wins over fuzzy match. 12 new tests covering exact match (mixed case + dashes), duplicate_in_batch ordering, fuzzy match (case-insensitive IOR, date-mismatch → new, ior-mismatch → new, missing field → new), invalid (gibberish), empty input, exact-wins-over-fuzzy precedence. 673/673 pass; lint + typecheck clean; build green (24 routes — pure module).
