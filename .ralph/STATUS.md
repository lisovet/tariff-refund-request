# Ralph Loop Status

**Updated**: 2026-04-21T09:35:00Z
**Branch**: claude/scaffold-platform
**Loop state**: active (iteration 33 → 34)

## Counts (v1 — task ids ≤ 86)

| Status | Count |
| --- | --- |
| completed | 33 |
| in-progress | 0 |
| pending | 53 |
| human-blocked | 0 |

## Quality gates (last run)

| Gate | Status |
| --- | --- |
| `npm test` | green — 58 files, 368 tests pass |
| `npm run lint` | clean |
| `npm run typecheck` | clean |
| `npm run build` | green — 17 routes |
| `npm run qa` (combined) | green |

## Last completed task

**#39 — Drizzle schema for cases + audit_log**

- `cases` columns: `id` (cas_*), `customerId` (FK→customers, set null), `screenerSessionId` (FK→screener_sessions, set null), `state` (typed enum of all 18 PRD-04 states, default `new_lead`), `tier` (smb|mid_market), `ownerStaffId` (FK→staff_users, set null), `createdAt`/`updatedAt`. Indexes on `state`, `owner`, `customer`.
- `audit_log` columns: `id` (aud_*), `caseId` (FK→cases RESTRICT, NOT NULL), `actorId`, `kind` (NOT NULL), `fromState`/`toState` (nullable enum), `payload` jsonb DEFAULT empty object, `occurredAt`. Indexes on `(case_id, occurred_at)` and `kind`.
- `drizzle/0003_cases_audit_log.sql` generated by drizzle-kit, then hand-augmented with the audit-log RLS DDL — `ENABLE + FORCE` row-level security; SELECT and INSERT policies open; UPDATE and DELETE policies pinned to `USING (false)` so the database itself rejects any modification or removal. Migration name normalized in `_journal.json`.
- `CASE_STATES`, `CaseRow`, `NewCaseRow`, `AuditLogRow`, `NewAuditLogRow` re-exported from `src/shared/infra/db/schema.ts` for downstream contexts (case-machine in #40+).
- 8 new tests including SQL-grep assertions that the RLS DDL is present and shaped correctly (`FOR DELETE USING (false)`).

## Human-verification still owes

- Apply migration to a real Postgres (`npm run db:migrate`); confirm DELETE on `audit_log` returns the RLS error.
- Confirm RLS policy semantics fit the production app role plan (PRD 10 mentions per-role read scoping; v1 keeps reads open inside the app boundary).

## Next eligible

Per dependency check (v1 only):
- Task #16 (`/pricing` page) — deps `[14, 36]` now satisfied. **Eligible — lowest id.**
- Task #40 (XState case machine) — deps satisfied.
- Task #44 (documents + recovery_sources schema) — deps satisfied.
- Task #49 (recovery routing — broker vs DIY) — deps satisfied.
- Task #67 (CAPE prep workflow scaffold) — deps satisfied.

Lowest-id eligible is **task #16** — `/pricing` page (now unblocked because #36 Stripe Checkout landed).

## Notes

- Wave 7 (Case state machine + audit log) 1/several started.
- Loop will continue with task #16 next iteration — back to the marketing surface to render the pricing ladder from `pricing.ts` (no hand-typed numbers).
