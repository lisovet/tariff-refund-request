# Ralph Loop Status

**Updated**: 2026-04-21T10:18:00Z
**Branch**: claude/scaffold-platform
**Loop state**: active (iteration 40 → 41)

## Counts (v1 — task ids ≤ 86)

| Status | Count |
| --- | --- |
| completed | 40 |
| in-progress | 0 |
| pending | 46 |
| human-blocked | 0 |

## Quality gates (last run)

| Gate | Status |
| --- | --- |
| `npm test` | green — 64 files, 450 tests pass |
| `npm run lint` | clean |
| `npm run typecheck` | clean |
| `npm run build` | green — 19 routes |
| `npm run qa` (combined) | green |

## Last completed task

**#44 — Drizzle schema for documents + recovery_sources**

- `documents` columns: id (`doc_*`), caseId (FK→cases RESTRICT, NOT NULL), storageKey (UNIQUE — one row per object in the bucket per ADR 006), filename, contentType, byteSize (bigint), sha256, uploadedBy (`customer|staff|system` enum), uploadedByActorId, version (default 1), supersedesId (lineage to prior version), createdAt.
  - Documents are immutable; new versions are separate rows.
  - UNIQUE `(case_id, sha256)` provides content-hash dedupe inside a case.
- `recovery_sources` columns: id (`rsrc_*`), caseId (FK→cases RESTRICT, NOT NULL), path (`broker|carrier|ace_self_export|mixed`), kind (`ace_export|broker_7501|broker_spreadsheet|carrier_invoice|paper_scan|other`), documentId (FK→documents RESTRICT, **NOT NULL** — provenance is never optional per `.ralph/PROMPT.md` hard rule), uploadedBy + uploadedByActorId, confidence (`pending|verified|rejected`, default `pending`), notes, createdAt. Indexes on `(case_id, created_at)`, `document_id`, `confidence`.
- `drizzle/0004_documents_recovery_sources.sql` generated by drizzle-kit; migration name normalized in `_journal.json`.
- Schema barrel re-exports row types + NEW row types + every enum constant (`DOCUMENT_KINDS`, `RECOVERY_PATHS`, `RECOVERY_SOURCE_CONFIDENCES`, `DOCUMENT_UPLOADERS`) for downstream contexts (recovery context lands in #45+).
- 12 new tests covering registry membership, every required column on both tables, both enum value sets, and migration SQL-grep checks (UNIQUE on storage_key, UNIQUE on (case_id, sha256)).

## Human-verification still owes

- Apply migration to a real Postgres; insert a sample document with a malformed `storage_key` and confirm the unique-violation surface is friendly.
- Review the `documents.supersedesId` lineage approach — currently a self-referential text column without a FK; consider tightening once the Recovery service lands in #45+.

## Next eligible

Per dependency check (v1 only):
- Task #45 (Recovery context — service surface, repo) — deps `[44]` satisfied. **Eligible — lowest id.**
- Task #49 (recovery routing — broker vs DIY) — deps satisfied.
- Task #55 (entries schema — uses recovery_sources for provenance) — deps satisfied.
- Task #67 (CAPE prep workflow scaffold) — deps satisfied.
- Task #72 (admin dashboard scaffold) — deps satisfied.

Lowest-id eligible is **task #45**.

## Notes

- Wave 8 (Recovery context) starts. Schema is the foundation; #45 builds the service surface; #49 adds routing logic.
- Loop will continue with #45 next iteration.
