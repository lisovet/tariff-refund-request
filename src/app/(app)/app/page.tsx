/*
 * Customer-authenticated app route group placeholder. Auth middleware
 * lands in a later task (PRD 01 / ADR 004).
 */

export default function AppHomePlaceholder() {
  return (
    <main className="min-h-[100dvh] bg-paper text-ink">
      <p className="p-8 font-mono text-sm">
        Customer app route group — pending PRD 01 / auth implementation.
      </p>
    </main>
  )
}
