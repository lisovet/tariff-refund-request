/*
 * Internal ops console route group placeholder. Role-gated middleware
 * lands in a later task (PRD 04).
 */

export default function OpsHomePlaceholder() {
  return (
    <main className="min-h-[100dvh] bg-paper text-ink">
      <p className="p-8 font-mono text-sm">
        Ops console route group — pending PRD 04 implementation.
      </p>
    </main>
  )
}
