// Component tests opt into jest-dom matchers (toBeDisabled,
// toBeInTheDocument, etc.) by importing this module via vitest's
// setupFiles. See vitest.config.ts.

import '@testing-library/jest-dom/vitest'
